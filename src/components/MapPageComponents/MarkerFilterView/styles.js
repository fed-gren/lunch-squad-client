import styled from "styled-components";

const MarkerFilter = styled.section`
  flex: 1;
  background-color: rgba(1, 1, 1, 0.4);
  width: 100%;
  position: relative;
  z-index: 1;
  visibility: hidden;
`;

export default { MarkerFilter };
