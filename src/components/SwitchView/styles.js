import styled from "styled-components";

const Switch = styled.div`
  & > label {
    display: flex;
    align-items: center;

    & * {
      font-size: 1rem;
    }
  }
`;

export default { Switch };
