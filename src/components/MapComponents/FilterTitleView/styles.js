import styled from "styled-components";

const FilterTitle = styled.div`
  height: 2rem;
  display: flex;
  align-items: center;
  & > h3 {
    font-size: 1.1rem;
    margin-left: 0.5rem;
  }
`;

export default { FilterTitle };
