import styled from "styled-components";

const ReviewItemScore = styled.div`
  flex: 1;
  font-size: 1rem;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px dotted green;

  & > div {
    margin: 0 0.4rem;
  }
`;

export default {
  ReviewItemScore
};
