import styled from "styled-components";

const ReviewItemScore = styled.div`
  flex: 1;
  font-size: 1rem;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: flex-end;

  & > div {
    margin-left: 0.8rem;
  }
`;

export default {
  ReviewItemScore
};
