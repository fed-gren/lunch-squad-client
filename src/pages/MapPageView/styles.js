import styled from "styled-components";

const MapPage = styled.section`
  /* TODO: remove border */
  border: 1px dotted gray;
  height: 100%;
  display: flex;
  flex-direction: column;
  position: relative;
`;

export default { MapPage };
